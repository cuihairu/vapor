global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: vapor-agent
    static_configs:
      - targets: ["agent:9700"]

  - job_name: vapor-controlplane
    static_configs:
      - targets: ["controlplane:8080"]
